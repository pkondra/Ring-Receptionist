{
  "name": "vozexo",
  "private": true,
  "scripts": {
    "sync:env": "node scripts/sync-env.mjs",
    "dev": "npm run dev:web & npm run dev:convex",
    "dev:web": "npm run sync:env && npm --prefix apps/web run dev",
    "dev:convex": "npx convex dev",
    "build": "npm run sync:env && npm --prefix apps/web run build",
    "start": "npm run sync:env && npm --prefix apps/web run start",
    "lint": "npm --prefix apps/web run lint",
    "typecheck": "npm --prefix apps/web run typecheck"
  },
  "devDependencies": {
    "convex": "^1.21.0",
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@clerk/shared",
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  },
  "dependencies": {
    "framer-motion": "^12.31.0"
  }
}
